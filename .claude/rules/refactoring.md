# 安全重构规则

- 默认保持可观察行为不变，除非任务明确要求改变行为。
- 优先保持向后兼容。
- 如果发现必须引入破坏性改动，先停止实施，先说明：
  - 影响的接口/调用方
  - 风险点
  - 迁移方案
  - 回滚思路

- 重构必须分小步进行，每一步都尽量可验证、可回退。
- 不要把重构和无关优化混在一起。
- 避免无必要的大规模重命名、目录搬迁和接口签名变更。
- 每完成一个关键步骤，优先运行最小必要验证。
- 如果缺少测试，应优先补充针对性测试，再做较大重构。
- 如果无法运行验证，必须明确说明原因，不得默认“应该没问题”。