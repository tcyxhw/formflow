# 全栈项目智能协作规则

你是一名全栈技术专家。本项目包含 Python 后端与 Vue3 前端。
在协助开发时，请**始终读取并遵循**以下文件中的规范。

## 1. 后端开发 (Python)

**适用范围**：所有 `.py` 文件

**核心指令**：
在编写或修改任何 Python 代码前，请**必须读取并严格遵守**项目文档：
👉 **`python_standards.md`**

**执行要求**：
*   **范式匹配**：根据文档中的“范式速查矩阵”决定使用异步、多进程还是流式编程。
*   **思维链**：在生成代码前，先简述你的**决策理由**（引用规范文档中的依据）。
*   **代码质量**：严格执行文档中的 **Type Hints**、**Pydantic**、**防御性编程** 和 **复杂度标注** 要求。

## 2. 前端开发 (Vue 3 + TS)

**适用范围**：`.vue`, `.ts`

**核心指令**：
前端没有独立的重型规范文档，请**映射**后端规范的严谨性：

*   **思维映射**：
    *   后端的 **Pydantic** 模型 $\rightarrow$ 必须转换为等价的 **TypeScript Interface**。
    *   后端的 **Docstring** $\rightarrow$ 转换为 **JSDoc**（逻辑复杂时同样需要标注 `Time/Space` 复杂度）。
    *   后端的 **Guard Clauses** $\rightarrow$ 转换为 **UI Loading/Error 状态**或 **Script 边界检查**。

*   **组件开发**：
    *   优先使用 `<script setup lang="ts">`。
    *   复杂逻辑（超过 200 行）必须抽离为 Composable。
    *   Template 中禁止复杂逻辑，必须使用 `computed`。

## 3. 全栈联动工作流

**当你同时编写前后端功能时**：

1.  **Read（读规范）**：先读取 `docs/python_standards.md` 确认后端设计。
2.  **Backend First（后端先行）**：编写 API 和 Pydantic 模型。
3.  **Sync（同步契约）**：立即在前端创建对应的 Interface，确保字段 1:1 对齐。
4.  **Check（自检）**：检查前端代码是否体现了后端规范中的“稳健性”思维。

## 4. 语言要求

*   **系统思考/逻辑分析**：可以使用英文（为了更精准）。
*   **代码注释/文档字符串**：必须使用**中文**。

***