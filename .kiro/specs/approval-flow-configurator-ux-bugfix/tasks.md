# 实现计划

- [x] 1. 编写缺陷条件探索测试
  - **Property 1: Bug Condition** - 审批流程配置器 UX 缺陷
  - **重要**：在实现修复之前编写此基于属性的测试
  - **目标**：表现缺陷存在的反例
  - **作用域 PBT 方法**：针对具体失败情况进行作用域限制，确保可重现性
  - 测试条件设置弹窗是否正确打开（来自设计中的缺陷条件）
  - 测试开始/结束节点是否不显示配置选项（来自设计中的缺陷条件）
  - 测试多条路由是否都被保留（来自设计中的缺陷条件）
  - 在未修复代码上运行测试
  - **期望结果**：测试失败（这证实了缺陷存在）
  - 记录发现的反例以理解根本原因
  - 任务完成时标记为完成，当测试被编写、运行并记录失败
  - _需求：1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 2. 编写保留属性测试（在实现修复之前）
  - **Property 2: Preservation** - 非缺陷条件行为保留
  - **重要**：遵循观察优先方法论
  - 观察：在未修复代码上，审批节点的配置正常工作
  - 观察：在未修复代码上，路由的创建和删除正常工作
  - 编写基于属性的测试：对于所有非缺陷条件的操作，系统行为应该保持不变（来自设计中的保留需求）
  - 验证测试在未修复代码上通过
  - _需求：3.1, 3.2, 3.3, 3.4_

- [-] 3. 修复审批流程配置器 UX 缺陷

  - [x] 3.1 修复开始/结束节点显示问题
    - 在 FlowNodeInspector.vue 中检查条件配置部分的渲染条件
    - 确保 `shouldShowConditionConfig` 方法对开始/结束节点返回 false
    - 验证条件配置部分的 `v-if` 指令正确排除开始/结束节点
    - _缺陷条件：isBugCondition(action) 其中 action.type = 'SELECT_NODE' AND action.nodeType IN ['start', 'end']_
    - _期望行为：系统不应该显示条件设置和路由属性配置选项_
    - _保留：审批节点应该继续显示这些配置选项_
    - _需求：2.2, 2.3, 3.1_

  - [ ] 3.2 修复路由覆盖问题
    - 检查 flowDraft store 中的 `updateRoute` 方法
    - 确保路由数组的更新只影响指定索引的路由
    - 验证路由的唯一标识符处理逻辑
    - 确保多条路由可以独立保存而不相互覆盖
    - _缺陷条件：isBugCondition(action) 其中 action.type = 'UPDATE_ROUTE' AND previousRouteExists_
    - _期望行为：所有路由都应该被独立保存_
    - _保留：路由的创建和删除应该继续正常工作_
    - _需求：2.4, 2.5, 3.1_

  - [x] 3.3 验证条件设置弹窗正确实现
    - 检查 FlowNodeInspector.vue 中的 `openConditionModal` 方法
    - 确保模态框的 `v-model:show` 绑定正确
    - 验证条件编辑器组件正确传递了所有必要的 props
    - 测试条件设置弹窗是否正确打开和关闭
    - _缺陷条件：isBugCondition(action) 其中 action.type = 'EDIT_CONDITION' AND action.nodeType = 'condition'_
    - _期望行为：应该打开一个模态框进行条件编辑_
    - _保留：其他节点类型的编辑应该继续正常工作_
    - _需求：2.1_

  - [ ] 3.4 验证缺陷条件探索测试现在通过
    - **Property 1: Expected Behavior** - 审批流程配置器 UX 缺陷修复
    - **重要**：重新运行第 1 步中的相同测试，不要编写新测试
    - 第 1 步中的测试编码了期望的行为
    - 当此测试通过时，它确认期望的行为得到满足
    - 运行第 1 步中的缺陷条件探索测试
    - **期望结果**：测试通过（确认缺陷已修复）
    - _需求：期望行为属性_

  - [ ] 3.5 验证保留测试仍然通过
    - **Property 2: Preservation** - 非缺陷条件行为保留
    - **重要**：重新运行第 2 步中的相同测试，不要编写新测试
    - 运行第 2 步中的保留属性测试
    - **期望结果**：测试通过（确认没有回归）
    - 确认所有测试在修复后仍然通过（没有回归）

- [ ] 4. 检查点 - 确保所有测试通过
  - 确保所有测试通过，如有问题请咨询用户
